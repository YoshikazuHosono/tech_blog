name: Send to Qiita

on:
  push:
    branches: [cicd_sample]

jobs:
  send-to-qiita:
    runs-on: ubuntu-latest
    steps:
      - run: echo $(git diff 92600a1ec009ac1e4585b0cd8894dc428c732a51 d5bbd9bd12e3c0e6f93a994c1eee72a7ef9a2ce8 --name-only --diff-filter=A -- '*.md')
      - run: echo "🍏 This job's status is ${{ job.status }}."
